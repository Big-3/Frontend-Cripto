import { CertificateUtils } from './certificate-utils.model';

describe('CertificateUtils', () => {
  it('should create an instance', () => {
    expect(new CertificateUtils()).toBeTruthy();
  });
});
